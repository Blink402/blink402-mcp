export interface RetryOptions {
    maxRetries?: number;
    initialDelayMs?: number;
    maxDelayMs?: number;
    backoffMultiplier?: number;
    shouldRetry?: (error: unknown, attempt: number) => boolean;
    onRetry?: (error: unknown, attempt: number, nextDelay: number) => void;
}
/**
 * Execute a function with exponential backoff retry logic
 * @param fn - The async function to execute
 * @param options - Retry configuration options
 * @returns The result of the function if successful
 * @throws The last error if all retries are exhausted
 */
export declare function withExponentialBackoff<T>(fn: () => Promise<T>, options?: RetryOptions): Promise<T>;
/**
 * Sleep for a specified number of milliseconds
 * @param ms - The number of milliseconds to sleep
 * @returns A promise that resolves after the specified time
 */
export declare function sleep(ms: number): Promise<void>;
/**
 * Retry helper specifically for Solana RPC calls
 * Handles common Solana RPC errors with appropriate retry logic
 */
export declare function retrySolanaRpc<T>(fn: () => Promise<T>, operation: string, options?: Partial<RetryOptions>): Promise<T>;
/**
 * Create a timeout promise that rejects after specified milliseconds
 * @param ms - Timeout in milliseconds
 * @param operation - Description of the operation (for error message)
 * @returns A promise that rejects with a timeout error
 */
export declare function timeout<T>(ms: number, operation: string): Promise<T>;
/**
 * Execute a promise with a timeout
 * @param promise - The promise to execute
 * @param ms - Timeout in milliseconds
 * @param operation - Description of the operation
 * @returns The result of the promise if it completes before timeout
 * @throws Timeout error if the promise takes too long
 */
export declare function withTimeout<T>(promise: Promise<T>, ms: number, operation: string): Promise<T>;
//# sourceMappingURL=retry-utils.d.ts.map