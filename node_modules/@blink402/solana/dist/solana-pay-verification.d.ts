import { Connection, PublicKey, Commitment } from '@solana/web3.js';
import BigNumber from 'bignumber.js';
export interface PaymentVerificationOptions {
    connection: Connection;
    reference: PublicKey;
    recipient: PublicKey;
    amount: bigint | BigNumber;
    splToken?: PublicKey;
    timeout?: number;
    commitment?: Commitment;
}
export interface PaymentVerificationResult {
    signature: string;
    blockTime?: number | null;
    slot: number;
    confirmationStatus?: string;
}
/**
 * Verify a payment using official Solana Pay methods with polling and proper error handling
 * Replaces custom verification implementation with battle-tested official methods
 */
export declare function verifyPaymentWithSolanaPay(options: PaymentVerificationOptions): Promise<PaymentVerificationResult>;
/**
 * Find a transaction by reference without validation
 * Useful for checking transaction status without validating amounts
 */
export declare function findTransactionByReference(connection: Connection, reference: PublicKey, options?: {
    commitment?: Commitment;
    limit?: number;
}): Promise<string | null>;
/**
 * Validate a transaction with retry logic for transient errors
 */
export declare function validateTransferWithRetry(connection: Connection, signature: string, expectedDetails: {
    recipient: PublicKey;
    amount: BigNumber;
    splToken?: PublicKey;
    reference?: PublicKey;
}, options?: {
    maxRetries?: number;
    retryDelay?: number;
    commitment?: Commitment;
}): Promise<void>;
//# sourceMappingURL=solana-pay-verification.d.ts.map